# This file is used to override the docker-compose.yml file for local development
# Warning: Should not be included in the repository when using CI/CD pipelines
services:
  db:
    env_file: .env
    ports:
      - "5432:5432"
  app:
    env_file: .env
    volumes:
      - .:/vocationalnyc
    ports:
      - "8000:8000"